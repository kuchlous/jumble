<!DOCTYPE html>
<html>
<head>
  <title>Jumble</title>
  <script src="http://www.google.com/jsapi" type="text/javascript"> </script> 
  <script type="text/javascript">
    google.load("jquery",    "1.7.1",  { uncompressed: true });
    google.load("jqueryui",    "1.8.16",  { uncompressed: true });
  </script> 
  <%= javascript_include_tag ('bootstrap/bootstrap') %>
  <%= javascript_include_tag ('application') %>
  <%= stylesheet_link_tag 'bootstrap', :media => "all" %>
  <%= stylesheet_link_tag 'application', :media => "all" %>
  <%= stylesheet_link_tag 'hangman', :media => "all" %>
  <%= stylesheet_link_tag 'find_words', :media => "all" %>
  <%= stylesheet_link_tag 'find_words_screen' %>
  <%= csrf_meta_tag %>
  <style type="text/css" media="print">
    @page
    {
      size: landscape;
      margin: 2cm;
    }
  </style>

</head>

<body>
<div class="wrapper">
  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <%= link_to "SpellApp", {:controller => "home", :action => "home"}, :class => "brand" %>
          <ul class="nav pull-left">
            <li <%=is_active_page?("WordBox") ? "class=active" : "" %> > <%= link_to "Make WordBox", {:controller => "teachers", :action => "input_word_box"} %> </li>
            <li <%=is_active_page?("AddWords") ? "class=active" : "" %> > <%= link_to "Add Weekly Words", {:controller => "teachers", :action => "add_words"} %> </li>
          </ul>
          <ul class="nav pull-right">
            <% if current_user %>
              <% if current_user.print_name == '' %>
              <li> <a href="/logout" id='my_logout'> Logout </a> </li>
              <% else %>
              <li class="dropdown" >
                <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.print_name %></a>
                <ul class="dropdown-menu">
                  <li> <a href="/logout" id='my_logout'> Logout </a> </li>
                </ul>
              </li>
              <% end %>
            <% end %>
          </ul>
      </div>
    </div>
  </div>

  <div id="allcontent">
    <div class="container" id="content">
    <% if flash[:error] %>
      <div class="alert-message error" data-alert="alert"">
        <a class="close" href="#">x</a>
        <p><strong><%= flash[:error] %> </strong></p>
      </div>
      <% flash[:error] = nil %>
    <% end %>
    <% if flash[:notice] %>
      <div class="alert-message success" data-alert="alert"">
        <a class="close" href="#">x</a>
        <p><strong><%= flash[:notice] %> </strong></p>
      </div>
      <% flash[:notice] = nil %>
    <% end %>

    <%= yield %>
    </div>
    <div id="browser-error">
       <div class="span6 offset3">
         <h1> We only support Chrome and Mozilla Firefox at this point. Please download from <a href="http://mozilla.org/en-US/firefox/new/"> Mozilla </a> or <a href="https://www.google.com/chrome"> Google </a> </h1>
       </div>
    </div>
  </div>
  <div class="push"> </div>
</div>
<% if !is_active_page?("LOGIN") %>
<footer>
  <hr>
  <%= link_to "About", :controller => "home", :action => "about" %>
  <%= link_to "Privacy", :controller => "home", :action => "privacy" %>
</footer>
<% end %>
</body>
</html>
