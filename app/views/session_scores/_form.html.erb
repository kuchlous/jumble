<%= form_for(@session_score) do |f| %>
  <% if @session_score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@session_score.errors.count, "error") %> prohibited this session_score from being saved:</h2>

      <ul>
      <% @session_score.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.number_field :user %>
  </div>
  <div class="field">
    <%= f.label :score %><br />
    <%= f.number_field :score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
