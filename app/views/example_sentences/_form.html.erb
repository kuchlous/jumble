<%= form_for(@example_sentence) do |f| %>
  <% if @example_sentence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@example_sentence.errors.count, "error") %> prohibited this example_sentence from being saved:</h2>

      <ul>
      <% @example_sentence.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tatoeba_id %><br />
    <%= f.number_field :tatoeba_id %>
  </div>
  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
