<%= form_for(@grade_week_word) do |f| %>
  <% if @grade_week_word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grade_week_word.errors.count, "error") %> prohibited this grade_week_word from being saved:</h2>

      <ul>
      <% @grade_week_word.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :grade_id %><br />
    <%= f.number_field :grade_id %>
  </div>
  <div class="field">
    <%= f.label :word_id %><br />
    <%= f.number_field :word_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
